<h1>Teams (<%= @teams.length %>)</h1>

<div class="teams-toolbar">
  <div class="teams-toolbar-left">
    <%= link_to "New Team", new_team_path, class: "btn btn-primary" %>
    <%= link_to "Export to Excel", export_teams_path, class: "btn" %>
  </div>
  <%= form_tag teams_path, method: :get, class: "search-form" do %>
    <%= text_field_tag :search, params[:search], placeholder: "Search teams...", class: "search-input" %>
    <%= submit_tag "Search", class: "btn btn-small" %>
    <% if params[:search].present? %>
      <%= link_to "Clear", teams_path, class: "btn btn-small" %>
    <% end %>
  <% end %>
</div>

<table class="teams-table">
  <thead>
    <tr>
      <th>Color</th>
      <th><%= sort_link "Name", "name" %></th>
      <th><%= sort_link "Abbr", "abbreviation" %></th>
      <th><%= sort_link "Coach Name(s)", "coach" %></th>
      <th><%= sort_link "Address", "address" %></th>
      <th><%= sort_link "Phone", "phone" %></th>
      <th><%= sort_link "Email", "email" %></th>
      <th><%= sort_link "Hours", "hours" %></th>
      <th><%= sort_link "Misc Expense", "misc_expense" %></th>
      <th>Actions</th>
    </tr>
  </thead>
  <tbody>
    <% @teams.each do |team| %>
      <tr>
        <td><span class="color-swatch" style="background-color: <%= team.color %>"></span></td>
        <td><%= team.name %></td>
        <td><%= team.abbreviation %></td>
        <td><%= team.coach %></td>
        <td><%= team.address %></td>
        <td><%= team.phone %></td>
        <td><%= team.email %></td>
        <td><%= "%.2f" % team.total_hours.to_f %></td>
        <td><%= number_to_currency(team.misc_expense) %></td>
        <td class="actions-cell">
          <%= link_to "Edit", edit_team_path(team), class: "btn btn-small" %>
          <%= button_to "Delete", team_path(team), method: :delete, class: "btn btn-small btn-danger",
              data: { turbo_confirm: "Delete #{team.name}?" } %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>
